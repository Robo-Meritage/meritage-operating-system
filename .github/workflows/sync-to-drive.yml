# DISABLED — do not auto-run.
#
# This key-based repo -> Google Drive sync was retired. Meritage's Google org
# blocks service-account key creation (iam.disableServiceAccountKeyCreation),
# which this approach required. The repo -> Drive mirror now runs via a Google
# Apps Script that executes under a user account (no key, no GCP), so this
# workflow and scripts/sync_to_drive.py are left only for reference.
#
# Runs only if triggered manually from the Actions tab.

name: Sync docs to Google Drive (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Repo->Drive mirroring is handled by a Google Apps Script."
